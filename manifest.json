{
  "manifest_version": "0.3",
  "name": "claude-memory",
  "display_name": "Claude Memory",
  "version": "1.0.0",
  "description": "Persistent memory for Claude across conversations using local markdown files",
  "long_description": "Every conversation with Claude starts fresh. This extension fixes that by storing context in simple markdown files on your machine and exposing them through the Model Context Protocol. Each new session can pick up where the last one left off. Memory files are plain markdown stored locally — your data never leaves your machine.",
  "author": {
    "name": "nocluetoday",
    "url": "https://github.com/nocluetoday"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nocluetoday/claude-memory-mcp"
  },
  "homepage": "https://github.com/nocluetoday/claude-memory-mcp",
  "documentation": "https://github.com/nocluetoday/claude-memory-mcp#readme",
  "support": "https://github.com/nocluetoday/claude-memory-mcp/issues",
  "icon": "icon.png",
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/index.js"],
      "env": {
        "CLAUDE_MEMORY_DIR": "${user_config.memory_directory}"
      }
    }
  },
  "tools": [
    {
      "name": "memory_read_soul",
      "description": "Read a persistent memory file (default: soul.md)"
    },
    {
      "name": "memory_write_soul",
      "description": "Write to a persistent memory file, overwriting existing content"
    },
    {
      "name": "memory_append_soul",
      "description": "Append content to a persistent memory file without overwriting"
    },
    {
      "name": "memory_list_files",
      "description": "List all memory files in the memory directory"
    },
    {
      "name": "memory_delete_file",
      "description": "Delete a memory file (requires explicit confirmation)"
    }
  ],
  "keywords": [
    "memory",
    "persistent",
    "context",
    "soul",
    "markdown",
    "local",
    "privacy"
  ],
  "license": "MIT",
  "user_config": {
    "memory_directory": {
      "type": "directory",
      "title": "Memory Directory",
      "description": "Directory where memory files are stored",
      "default": "${HOME}/.claude-memory",
      "required": false
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=18.0.0"
    }
  },
  "privacy_policies": [
    "https://github.com/nocluetoday/claude-memory-mcp/blob/main/PRIVACY.md"
  ]
}
